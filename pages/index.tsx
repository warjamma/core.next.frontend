import React from "react";

import { Cards, Layout, Main } from "@components";

const Home: React.FC = () => {
    return (
        <Layout>
            <Main />
            <Cards />
        </Layout>
    );
};

export default Home;
